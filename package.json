{
  "name": "sajt",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "npm run generate-sitemap && astro build",
    "preview": "npm run build && wrangler dev",
    "astro": "astro",
    "generate-types": "wrangler types",
    "generate-sitemap": "node .github/scripts/generate-sitemap.mjs",
    "deploy": "npm run build && wrangler deploy",
    "indexnow": "node .github/scripts/submit-indexnow.mjs"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^14.1.3",
    "astro": "^7.0.7"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.2",
    "tailwindcss": "^4.3.2",
    "wrangler": "^4.110.0"
  }
}
